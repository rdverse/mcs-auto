# Workflow Submodule Placeholder

Add workflow implementation assets (JS/Python/PowerShell) as a Git submodule.

Example:
```bash
git submodule add <workflow-repo-url> /home/runner/work/mcs-auto/mcs-auto/workflows/github-submodule
```
